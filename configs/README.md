
# Configurations

This directory is reserved for custom evaluation hyperparameters.

By default, the evaluation pipeline uses configuration files located inside `javisbench/configs/`.  
If you need to override any parameter (e.g., CAVP model settings), place your modified YAML files here and adjust the script arguments accordingly.
